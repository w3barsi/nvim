return {
    "echasnovski/mini.nvim",
    config = function()
        -- require("mini.surround").setup({
        --     n_lines = 5000,
        -- })
        -- require("mini.ai").setup({
        --     n_lines = 5000,
        -- })
    end,
}
